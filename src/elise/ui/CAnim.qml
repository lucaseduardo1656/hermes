import QtQuick
import Elise

// Port of Caelestia's components/CAnim.qml — the default colour animation.
ColorAnimation {
    duration: Tokens.anim.durations.expressiveSlowEffects
    easing: Tokens.anim.expressiveSlowEffects
}
